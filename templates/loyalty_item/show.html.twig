{% extends 'base.html.twig' %}

{% block title %}LoyaltyItem{% endblock %}

{% block body %}
    <h1>LoyaltyItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loyalty_item.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('loyalty_item_index') }}">back to list</a>

    <a href="{{ path('loyalty_item_edit', {'id': loyalty_item.id}) }}">edit</a>

    {{ include('loyalty_item/_delete_form.html.twig') }}
{% endblock %}
