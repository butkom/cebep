<!DOCTYPE html>

<title>Edit PlayerGuild</title>

{% block body %}
    <h1>Edit PlayerGuild</h1>

    {{ include('player_guild/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('player_guild_index') }}">back to list</a>

    {{ include('player_guild/_delete_form.html.twig') }}
{% endblock %}
