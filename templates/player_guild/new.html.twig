<!DOCTYPE html>

<title>New PlayerGuild</title>

{% block body %}
    <h1>Create new PlayerGuild</h1>

    {{ include('player_guild/_form.html.twig') }}

    <a href="{{ path('player_guild_index') }}">back to list</a>
{% endblock %}
