<!DOCTYPE html>

<title>Guild</title>

{% block body %}
    <h1>Guild</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guild.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ guild.name }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ guild.shortName }}</td>
            </tr>
            <tr>
                <th>Server</th>
                <td>{{ guild.server }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('guild_index') }}">back to list</a>

    <a href="{{ path('guild_edit', {'id': guild.id}) }}">edit</a>

    {{ include('guild/_delete_form.html.twig') }}
{% endblock %}
