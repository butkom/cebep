<form method="post" action="{{ path('guild_champ_delete', {'id': guild_champ.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guild_champ.id) }}">
    <button class="btn">Delete</button>
</form>
