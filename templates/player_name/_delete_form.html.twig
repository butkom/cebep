<form method="post" action="{{ path('player_name_delete', {'id': player_name.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player_name.id) }}">
    <button class="btn">Delete</button>
</form>
