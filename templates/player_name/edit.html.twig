<!DOCTYPE html>

<title>Edit PlayerName</title>

{% block body %}
    <h1>Edit PlayerName</h1>

    {{ include('player_name/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('player_name_index') }}">back to list</a>

    {{ include('player_name/_delete_form.html.twig') }}
{% endblock %}
