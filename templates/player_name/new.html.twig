<!DOCTYPE html>

<title>New PlayerName</title>

{% block body %}
    <h1>Create new PlayerName</h1>

    {{ include('player_name/_form.html.twig') }}

    <a href="{{ path('player_name_index') }}">back to list</a>
{% endblock %}
