<form method="post" action="{{ path('player_lvl_delete', {'id': player_lvl.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player_lvl.id) }}">
    <button class="btn">Delete</button>
</form>
